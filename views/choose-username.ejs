<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Elige tu nombre de usuario</title>
    <link rel="stylesheet" href="/css/choose-username.css" />
  </head>
  <body>
    <div class="choose-username-container">
      <h1>Elige tu nombre de usuario</h1>
      <form method="POST" action="/choose-username">
        <input type="text" name="userName" placeholder="Nombre de usuario" required/>
        <input type="password" name="password" placeholder="Contraseña" required />
        <button type="submit">Continuar</button>
      </form>
      <% if (error === 'username_taken') { %>
        <p>Ese nombre de usuario ya está en uso.</p>
        <% } else if (error === 'missing_data') { %>
        <p>Faltan datos obligatorios.</p>
        <% } else if (error === 'server') { %>
        <p>Ocurrió un error en el servidor. Intenta de nuevo.</p>
        <% } %>
    </div>
  </body>
</html>
